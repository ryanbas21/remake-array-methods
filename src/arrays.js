function map(fn, arr) {}

function filter(fn, arr) {}

function every(fn, arr) {}

function some(fn, arr) {}

function reduce(fn, start, arr) {}

// don't use the native array length
function length(arr) {}

function shift(arr) {}

function unshift(arr) {}

function pop(arr) {}

export { shift, unshift, length, pop, filter, map, some, every, reduce };
